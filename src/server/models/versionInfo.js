'use scrict';

var VersionInfo = (function () {
    function VersionInfo(parameters) {
        this.application = parameters.application;
    }
    return VersionInfo;
})();

module.exports = {    
  versionInfo : Ver
};

var VersionInfo = (function () {
    function VersionInfo(parameters) {
        this.application = parameters.application;
    }
    return VersionInfo;
})();